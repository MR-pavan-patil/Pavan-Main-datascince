# Creating a vector for daily temperatures
temperatures <- c(72, 68, 74, 80, 76, 82, 79)

# Building a list for book information
books <- list(
  title = c("The Catcher in the Rye", "To Kill a Mockingbird", "1984"),
  author = c("J.D. Salinger", "Harper Lee", "George Orwell"),
  year = c(1951, 1960, 1949)
)

# Creating a matrix for student grades
student_grades <- matrix(c(85, 90, 78, 92, 88, 76), nrow = 2, ncol = 3)

# Constructing a data frame for employee information
employees <- data.frame(
  name = c("Alice", "Bob", "Charlie"),
  age = c(30, 25, 28),
  department = c("HR", "IT", "Finance")
)